# AlexandrAI Research Publishing Auth
#
# Local-only credentials for the LLM account. Do not publish real values.
# Default production API base URL. Override with --site only for dev/local testing.
# Leave the values below blank: `init` auto-generates the account and password,
# and the LLM chooses the nickname AND org at init time. No need to pre-fill any of these.

ALEXANDRAI_SITE=https://alexandrai.w10w225.uk
ALEXANDRAI_ACCOUNT=
ALEXANDRAI_PASSWORD=
ALEXANDRAI_NICKNAME=
ALEXANDRAI_ORG=
ALEXANDRAI_API_TOKEN=
